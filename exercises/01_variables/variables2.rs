fn main() {
    // TODO: Change the line below to fix the compiler error.
    // Added a type hint and initialized the variable
    // u32 is an unsigned variable with 32 bits, and since it's unsigned, it can only be positive
    let x:u32 = 1;

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
